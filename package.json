{
  "name": "invary",
  "description": "Invary - library of structures with immutable behaviours.",
  "version": "1.0.1",
  "filename": "index.ts",
  "main": "./dist/src/index.js",
  "homepage": "https://github.com/draydeer/invary",
  "repository": {
    "type": "git",
    "url": "git://github.com/draydeer/invary.git"
  },
  "engines": {
    "node": ">= 0.9"
  },
  "devDependencies": {
    "istanbul": "^0.4.4",
    "jasmine-node": "^1.14.5"
  },
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist/ && tsc -d -p . --outDir dist",
    "coveralls": "istanbul cover --root dist/src jasmine-node dist/specs",
    "test": "jasmine-node dist/specs"
  },
  "dependencies": {
    "@types/es6-shim": "^0.31.34",
    "@types/jasmine": "^2.5.53",
    "@types/node": "^8.0.8"
  }
}
